import 'package:flutter/material.dart';

class Sensor_Info extends StatelessWidget {
  const Sensor_Info({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('센서 지도'),
      ),
    );
  }
}