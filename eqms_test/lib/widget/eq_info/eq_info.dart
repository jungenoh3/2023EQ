import 'package:flutter/material.dart';

class EQ_Info extends StatelessWidget {
  const EQ_Info({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('지진 정보 지도'),
      ),
    );
  }
}