import 'package:flutter/material.dart';

class Sensor_Map extends StatelessWidget {
  const Sensor_Map({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('센서 현황'),
      ),
    );
  }
}